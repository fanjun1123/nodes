#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_nounou@1.2.1@nounou/bin/nounou" "$@"
  ret=$?
else 
  node  "$basedir/../../../_nounou@1.2.1@nounou/bin/nounou" "$@"
  ret=$?
fi
exit $ret
